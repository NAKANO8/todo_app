# Implementation Plan

- [ ] 1. Foundation: アカウント状態カラムとユーザー情報基盤
- [x] 1.1 usersテーブルにアカウント状態を追加する
  - 新規登録および本機能導入前から存在するアカウントが既定で有効な状態になるよう、アカウント状態を保持するカラムを追加する
  - Observable: 開発DBを作り直すと、新規・既存相当のアカウントのいずれも既定で有効な状態になっていることを確認できる
  - _Requirements: 3.1, 3.2_

- [x] 1.2 ユーザー情報の型定義と、登録されている全ユーザーを取得する処理を拡張する
  - ユーザーを表す型にアカウント状態を含め、登録されている全ユーザーをロール・アカウント状態付きで取得する処理を新設する。パスワードハッシュは含めない
  - Observable: 全ユーザー取得を呼び出すと、各ユーザーのロールとアカウント状態を含む一覧が返り、パスワードハッシュがレスポンスに含まれないことを確認できる
  - _Requirements: 1.1, 1.2_
  - _Depends: 1.1_

- [x] 1.3 ロール変更を、最後の有効な管理者を保護しつつ適用する更新処理を実装する
  - 対象ユーザーの存在確認と「対象が最後の有効な管理者かどうか」の判定を単一の更新操作でアトミックに行い、適用できたかどうかを呼び出し元が判別できる形で結果を返す。判定は要求した本人が対象かどうかを区別しない
  - 値が変化しない更新の再送でも「適用できた」と正しく判別できることを確認できる（既に同じロールへの変更を繰り返しても、対象なし・保護違反と誤認しない）
  - Observable: 対象が最後の有効な管理者である場合、降格方向の変更は適用されず、それ以外の場合は正しく適用されることを確認できる
  - _Requirements: 2.1, 7.1, 7.3, 7.4_
  - _Depends: 1.1_

- [x] 1.4 アカウント状態変更を、最後の有効な管理者を保護しつつ適用する更新処理を実装する
  - 1.3と同じ保護パターンで、無効化方向の変更にのみ最後の管理者チェックを適用し、再有効化は無条件で適用する
  - 値が変化しない無効化の再送でも「適用できた」と正しく判別できることを確認できる（既に無効化済みの対象への再送を、対象なし・保護違反と誤認しない）
  - Observable: 対象が最後の有効な管理者である場合、無効化方向の変更は適用されず、それ以外の場合は正しく適用されることを確認できる
  - _Requirements: 4.1, 5.1, 7.2, 7.3, 7.4_
  - _Depends: 1.1_

- [ ] 2. Core: 管理者による認証・ユーザー管理のビジネスロジック
- [x] 2.1 (P) 無効化されたアカウントでのログインを拒否する
  - ログイン処理で、パスワード一致確認後にアカウント状態を確認し、無効化されている場合はログインを拒否する
  - Observable: 無効化されたアカウントに正しいパスワードでログインしようとすると拒否され、再有効化された後は通常のアカウントと同様にログインできることを確認できる
  - _Requirements: 4.3, 5.2_
  - _Boundary: AuthService_
  - _Depends: 1.1_

- [x] 2.2 (P) 管理者専用の認可判定を共通化する
  - 認証済みかつ管理者ロールであることを確認する処理を、既存の管理者専用エンドポイントと今後追加するエンドポイントの両方から使える形に切り出す
  - 既存の管理者専用エンドポイントの拒否条件・レスポンス内容が変わっていないことを確認できる
  - Observable: 管理者以外・未認証からのリクエストが、既存・新規いずれの管理者専用エンドポイント経由でも同様に拒否されることを確認できる
  - _Requirements: 6.1, 6.2_
  - _Boundary: AdminGuard_

- [x] 2.3 (P) ユーザー一覧・ロール変更・アカウント状態変更のサービスを実装する
  - 一覧取得、ロール変更、アカウント状態変更（無効化・再有効化）の業務ロジックを実装する。無効化する場合は既存のセッション強制無効化機能を呼び出し、それが例外を投げた場合は状態変更を取り消さずに例外をそのまま呼び出し元へ伝える
  - 唯一の管理者に対する降格・無効化要求（自分自身に対する要求・他の管理者に対する要求のいずれも）が拒否されることを確認できる
  - Observable: 通常のユーザーに対する一覧取得・ロール変更・無効化・再有効化がそれぞれ意図した結果を返すことを確認できる
  - _Requirements: 1.1, 1.2, 2.1, 4.1, 4.2, 5.1, 7.1, 7.2, 7.3, 7.4_
  - _Boundary: AdminUserService_
  - _Depends: 1.2, 1.3, 1.4_

- [ ] 3. Integration: 管理者向けユーザー管理エンドポイントを実装し、アプリケーションに登録する
  - ユーザー一覧取得・ロール変更・アカウント状態変更のエンドポイントを追加し、2.2の認可判定を適用してアプリケーションに登録する。自分自身のアカウントを無効化した場合は、そのリクエスト自身の認証状態も即座に失効させる
  - ロール変更後、対象ユーザーが自分のアカウント情報を取得すると新しいロールが反映されていることを確認できる
  - Observable: 管理者が自分自身のアカウントを無効化すると、レスポンス後に同じセッションでの以降のリクエストが未認証として扱われることを確認できる
  - _Requirements: 1.1, 2.1, 2.2, 4.1, 4.2, 5.1, 6.1, 6.2_
  - _Depends: 2.2, 2.3_

- [ ] 4. Integration: 管理者画面（todo-web）
- [ ] 4.1 (P) 管理者向けAPIクライアントと型を追加する
  - ユーザー一覧取得・ロール変更・アカウント状態変更を呼び出すクライアント関数と、対応する型を追加する
  - Observable: 一覧取得のクライアント関数を呼ぶと、管理者向けエンドポイントから実際にユーザー一覧が取得できることを確認できる
  - _Requirements: 1.1, 1.2, 2.1, 4.1, 5.1_
  - _Boundary: AdminApi_
  - _Depends: 3_

- [ ] 4.2 (P) 管理者以外のアクセスを制限するミドルウェアを拡張する
  - 認証結果のキャッシュにロール情報も含め、管理者向け画面のパスは管理者ロールを持たないユーザーからのアクセスを一般ユーザー向け画面へリダイレクトする
  - Observable: 一般ユーザーが管理者向け画面のURLへ直接アクセスすると、一般ユーザー向けの画面にリダイレクトされることを確認できる
  - _Requirements: 6.1, 6.2_
  - _Boundary: Middleware_

- [ ] 4.3 管理者画面のユーザー一覧・操作UIを実装する
  - ユーザー一覧を表示し、各ユーザーに対してロール変更・無効化・再有効化を行える画面を実装する
  - Observable: 一覧に各ユーザーのロールとアカウント状態が表示され、操作後はその結果を反映した状態が一覧に表示されることを確認できる
  - _Requirements: 1.1, 1.2, 2.1, 4.1, 5.1_
  - _Depends: 4.1_

- [ ] 5. Validation: 同時実行・回帰の確認
- [ ] 5.1 同時実行下でも最後の管理者保護が破られないことを確認する
  - 管理者がちょうど2人の状態で、両者が同時に自分自身を降格させる場合と、互いを降格させ合う場合の両方について、片方のみ成功し有効な管理者が必ず1人残ることを確認する検証を用意する
  - Observable: 上記いずれの同時実行パターンでも検証が成功し、有効な管理者が0人になる結果が発生しないことを確認できる
  - _Requirements: 7.1, 7.2, 7.3, 7.4_
  - _Depends: 3_

- [ ] 5.2 既存のセッション無効化機能に対する回帰がないことを確認する
  - 認可判定の共通化後も、既存の管理者向けセッション無効化エンドポイントの拒否条件・成功時の結果が変わっていないことを確認する
  - Observable: 既存のセッション無効化に関する検証が変更後もすべて成功することを確認できる
  - _Requirements: 6.1, 6.2_
  - _Depends: 2.2_

- [ ] 5.3 アカウント無効化からログイン拒否までの一連の流れを確認する
  - 管理者が対象ユーザーを無効化した後、対象ユーザーの既存のログイン状態が失効し、かつ新たなログイン試行も拒否されることを確認する検証を用意する
  - Observable: 無効化後、対象ユーザーの既存のリクエストが未認証として扱われ、かつ新たなログイン試行も拒否されることを確認できる
  - _Requirements: 4.2, 4.3_
  - _Depends: 3_

## Implementation Notes
- `docs/database-schema.md`のER図・テーブル定義更新（`role`・`status`の反映）は非コードのドキュメント作業のため、本タスク一覧には含めない。1.1と合わせて別途対応すること。
- **デプロイ順序の注意（design.md参照）**: 1.1のマイグレーション（本番DBへの`status`カラム追加）は、2.1を含む新アプリコードのデプロイより先に（最低でも同時に）適用すること。ログイン処理が無条件に`status`を読むため、カラムが存在しない状態で新コードが先に動くと、管理者機能に限らず全ユーザーのログインが失敗する。デプロイ手順（CD）側の対応が必要な場合は別途チケット化する。
- 5.1で検証する「対象がちょうど2人の管理者で相互に降格させ合う」ケースは、1.3/1.4で実装するアトミックな更新（対象行基準・`updated_at`明示によるaffectedRows整合）が正しく動作していることが前提。1.3/1.4のレビュー時にこの観点を含めること。
- **1.3実装時に判明**: design.mdのSQL案（`EXISTS (SELECT 1 FROM users WHERE ...)`が更新対象と同じテーブルを参照する形）はMySQL 8.0で`ERROR 1093: You can't specify target table 'users' for update in FROM clause`になり実行不可。サブクエリを導出テーブル（`SELECT 1 FROM (SELECT id FROM users WHERE ...) AS other_active_admins`）で包む標準的な回避策で対応済み（判定ロジック自体は不変）。1.4の`updateStatus`でも同じ回避策を使うこと。
- **2.3実装時に判明**: design.mdの`AdminUserServiceType`コード例・シーケンス図は`changeRole(requesterId, targetUserId, newRole)`のように`requesterId`を含む形で書かれているが、これはInvariants節の文章（`requesterId`は不変条件の判定には使わず、コントローラー側の自己ターゲット時`session.destroy()`判定にのみ使う）および既にマージ済みの`AuthRepository.updateRole`/`updateStatus`（`requesterId`を取らない、対象行のみで判定する契約）と矛盾する古い記述。実装は`requesterId`なしの`changeRole(targetUserId, newRole)`/`changeStatus(targetUserId, newStatus)`とした（レビューでも独立に正しいと確認済み）。design.mdのコード例は将来修正で構わないが、tasks.mdの記述を優先する。
