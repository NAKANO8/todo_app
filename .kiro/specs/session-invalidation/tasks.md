# Implementation Plan

- [ ] 1. Foundation: Redis基盤のセットアップ
- [x] 1.1 Docker Composeとtodo-apiの環境変数にRedisサービスを追加する
  - イメージとヘルスチェックを備えたRedisサービスを追加し、dev用のポート公開・prod用のネットワーク配線をそれぞれ行う
  - dev/prod/test向けの環境設定ファイルすべてにRedis接続先（ホスト・ポート）を追加し、ユニットテスト・結合テストの実行環境からもRedisに到達できるようにする
  - Observable: dev環境で起動するとRedisコンテナが立ち上がりヘルスチェックが通り、テスト実行環境からも同じRedisに接続できる
  - _Requirements: 1.1, 4.1_
- [x] 1.2 todo-apiにRedis接続を登録する
  - 公式のRedis接続プラグインを追加し、環境変数から読み取った接続先でtodo-api全体から共有できる接続を確立する
  - Observable: アプリ起動時にRedis接続が確立し、共有クライアント経由でコマンドを実行できる
  - _Requirements: 1.1, 4.1_
  - _Depends: 1.1_

- [ ] 2. Core: Redisセッションストアの自前実装
- [x] 2.1 (P) セッションプラグインが要求するストア契約を満たすRedisセッションストアを自前実装する
  - セッションの取得・保存・破棄の3操作のみを実装し、既存の外部ストアパッケージを採用しなかった理由と、実装が満たす契約の内容を、実装を知らない読み手にも分かる形でコード冒頭に明記する
  - Observable: 単体テストで、保存したセッションが取得でき、破棄した後は取得結果が「セッションなし」になることを確認できる
  - _Requirements: 1.1, 4.1, 4.3_
  - _Boundary: RedisSessionStore_
- [x] 2.2 セッションプラグインの既定ストアを2.1のRedisストアに切り替える
  - セッションプラグインの登録設定を変更し、既定のインメモリストアをRedisストアに置き換える
  - Observable: ログイン後にtodo-apiプロセスを再起動しても、Redis上にセッションデータが残っており再起動後も認証状態が維持される（既定のインメモリストアでは再起動で消えていた挙動が変わる）
  - _Requirements: 1.1, 4.1_
  - _Depends: 1.2, 2.1_

- [x] 3. (P) ユーザー単位のセッション逆引き索引リポジトリを実装する
  - あるユーザーに紐づく有効なセッションの集合を追加・削除・列挙・全削除できるようにする
  - セッション実体そのものの破棄は2.1で実装したストアに委譲し、キー命名等の重複実装を避ける
  - Observable: 単体テストで、登録したセッションが一覧に現れ、削除した分だけ一覧から消え、同じユーザーの他のセッションは影響を受けないことを確認できる
  - _Requirements: 1.1, 1.3, 2.2, 4.2_
  - _Boundary: SessionRepository_
  - _Depends: 2.1_

- [ ] 4. Core: ログイン・自己ログアウトでの索引維持
- [x] 4.1 ログイン時に発行されたセッションを逆引き索引へ登録する
  - ログイン成功時に、そのユーザーのセッション一覧へ今回発行されたセッションを追加する
  - Observable: ログイン直後、そのユーザーの索引に今回発行されたセッションが含まれることを確認できる
  - _Requirements: 2.2_
  - _Depends: 3_
- [x] 4.2 自己ログアウト時に該当セッションのみを索引から除去する
  - 自己ログアウト処理で、既存のセッション破棄と合わせて、そのセッションだけを索引から取り除く
  - Observable: 2つのセッションを持つユーザーが片方だけログアウトすると、索引にはもう片方のセッションだけが残ることを確認できる
  - _Requirements: 2.1, 2.2_
  - _Depends: 3_

- [x] 5. (P) 管理者による対象ユーザーのセッション無効化サービスを実装する
  - 対象ユーザーの索引を取得し、含まれる全セッションを破棄した上で索引自体をクリアする手順を実装する
  - 対象に有効なセッションが1件もない場合は例外を投げず、無効化件数0として扱う
  - 対象が呼び出し元の管理者自身であっても特別な分岐を設けない
  - Observable: 単体テストで、複数セッションを持つ対象に実行すると全セッションが破棄され索引が空になり正しい件数が返る。セッションが無い対象に実行してもエラーにならず件数0が返ることを確認できる
  - _Requirements: 1.1, 1.3, 3.3_
  - _Boundary: SessionService_
  - _Depends: 3_

- [ ] 6. Core: 管理者専用APIエンドポイント
- [x] 6.1 (P) 認証済みかつ管理者ロールであることを確認するアクセス制御を実装する
  - 後続タスクで追加するエンドポイントのルート内に、リクエスト送信者が認証済みでロールが管理者であることを確認する処理を用意する
  - 条件を満たさない場合は後続の処理（無効化サービスの呼び出し）に進ませない
  - Observable: 管理者以外からのリクエストは拒否され、未認証のリクエストも拒否されて、いずれもサービス呼び出しまで到達しないことをテストで確認できる
  - _Requirements: 3.1, 3.2_
  - _Boundary: AdminRoleGuard_
- [x] 6.2 管理者向けセッション無効化エンドポイントを実装する
  - 対象ユーザーを指定してセッションを無効化するエンドポイントを追加し、6.1のアクセス制御を通過したリクエストから5のサービスを呼び出し、無効化件数をレスポンスとして返す
  - Observable: 管理者権限で呼び出すと無効化件数を含む成功レスポンスが返り、対象ユーザーの以後のリクエストが未認証になる
  - _Requirements: 1.1, 1.3, 3.1, 3.2, 3.3_
  - _Depends: 5, 6.1_

- [x] 7. (P) todo-web側の認証結果キャッシュのTTLを短縮する
  - 認証結果を一定時間キャッシュしている仕組みのキャッシュ保持時間を、無効化の反映が数秒程度の遅延に収まる値まで短縮する
  - Observable: 変更後、キャッシュされた認証結果は新しいキャッシュ保持時間の経過後、必ずFastify側へ再問い合わせされることを確認できる
  - _Requirements: 1.2_
  - _Boundary: todo-web Middleware_

- [ ] 8. Integration: 全体配線と結合確認
- [x] 8.1 管理者向けエンドポイントをアプリケーションに登録し、複数セッションに対する無効化フローを結合テストで確認する
  - 6.2で実装したエンドポイントをアプリケーションに登録する
  - 同一ユーザーで2つのセッション（デバイスA・デバイスB相当）を作成し、管理者による無効化を実行した後、両方のセッションがその後の認証確認で未認証として扱われることを確認する結合テストを用意する
  - Observable: 結合テストが成功し、無効化後は両方のセッションを使ったリクエストが未認証として扱われる
  - _Requirements: 1.1, 1.2, 4.2_
  - _Depends: 2.2, 4.1, 4.2, 6.2_
- [x] 8.2 自己ログアウトが他デバイスのセッションに影響しないことを結合テストで確認する
  - 同一ユーザーの2セッションのうち1つだけを自己ログアウトさせ、もう片方のセッションが引き続き認証済みとして扱われることを確認する結合テストを用意する
  - Observable: 結合テストが成功し、ログアウトしていない方のセッションは引き続き認証済みとして扱われる
  - _Requirements: 2.1, 2.2_
  - _Depends: 4.2_

- [ ] 9. 複数todo-apiインスタンス構成でのセッション一貫性を確認する
  - 同一のRedisを指す2つのtodo-apiプロセスを起動し、一方のプロセスで作成したセッションをもう一方のプロセスも有効なセッションとして認識すること、また一方のプロセス経由で行った無効化がもう一方のプロセスからのリクエストにも反映されることを確認する検証手順を用意する
  - Observable: インスタンスBが、インスタンスAで作成したセッションを有効なセッションとして認識し、インスタンスA経由での無効化実行後はインスタンスB経由のリクエストでも同じセッションが未認証になることを確認できる
  - _Requirements: 4.1, 4.2, 4.3_
  - _Depends: 8.1_

## Implementation Notes
- 1.1: `.github/workflows/ci.yml`にはまだ`redis`サービスが無く、テストジョブの環境変数もMySQL専用でRedis到達性が無い。Redisに依存するテスト(2.1, 3, 5, 8.1, 8.2, 9)を追加するタスクで、CIワークフローへの`redis`サービス追加も合わせて対応する必要がある。
  - **追記**: 未対応のまま放置していたところ、CI上で`auth.api.test.ts`/`todos.api.test.ts`（`buildApp()`経由で実際の`@fastify/redis`接続を要求する既存の結合テスト）が`beforeAll`で10秒タイムアウトして実際に失敗した。`REDIS_HOST`未設定時のフォールバック先`127.0.0.1:6379`にCI上でRedisが存在しないため、`@fastify/redis`のプラグイン登録が接続待ちのままハングしていた。`mysql`サービスと同じパターンで`redis`サービス（`redis:7-alpine`、`redis-cli ping`ヘルスチェック）と`REDIS_HOST`/`REDIS_PORT`環境変数を追加して解消した。「後で対応すればいい」と流した既知のギャップが、実際にCIを壊す形で顕在化した一例。
- サンドボックス環境ではDocker/実Redisが使えないため、単体・結合テストは`ioredis-mock`（devDependency、388 stars・2025年10月最終リリース、SADD/SREM/SMEMBERS/GET/SET/DEL対応確認済み）を使って検証する。実Redisでの最終確認は別途手元環境で行う。
- `ioredis-mock`はインスタンス間でバックエンドを共有するため、複数テストで同じuserId/sessionIdを使う場合は`beforeEach`で`flushall()`する必要がある（4.1/4.2実装時に判明）。
- 4.1/4.2: `SessionRepository`は`app.redis`に依存するためモジュール読み込み時点では構築できず、`repositories/sessionRepositoryInstance.ts`で`buildApp()`内から一度だけ初期化するシングルトンホルダーを新設した。レビューで「`app.decorate`の方がFastify流だが、これも妥当」と評価。また`logout`の`untrackSession`呼び出しにはtry/catchが無く、失敗時は`login`等と違うエラー形状（Fastify既定の500）になる点が非ブロッキングの指摘として残っている（followupの余地あり）。
